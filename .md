npm init -y
npm install express
npm install nodemon
"scripts": {
"start": "node src/server.js",
"dev": "nodemon src/server.js",
"test": "echo \"Error: no test specified\" && exit 1"
},

connect to database
npm install pg
"development": {
"username": "postgres",
"password": "123",
"database": "express_practice",
"host": "127.0.0.1",
"dialect": "postgres"
},

connect to sequelize and generate folder
npm install sequelize sequelize-cli pg pg-hstore
npx sequelize-cli init

and go to config.json and fill database name

generate model and migration
npx sequelize-cli model:generate --name User --attributes name:string

generate table in database
npx sequelize-cli db:migrate

// install bcryptjs
npm install bcryptjs

// login with jwt
npm install jsonwebtoken

// install dotenv
npm install dotenv
create .env file

// undo the migration
npx sequelize-cli db:migrate:undo
npx sequelize-cli db:migrate

productImage  
npm i express-fileupload
npm i uuid

generate doc
using docxtemplater
need to install
npm install docxtemplater fs path pizzip dayjs

npm install cors

add new migration
npx sequelize-cli migration:generate --name add-email-to-user
